---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: components.experiments.illm.io
spec:
  group: experiments.illm.io
  names:
    kind: Component
    listKind: ComponentList
    plural: components
    singular: component
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.sources[*]
      name: Sources
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Component is the Schema for the components API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ComponentSpec defines the desired state of Component
            properties:
              description:
                description: Description of the component
                type: string
              observability:
                description: Observability configuration for this component
                properties:
                  metricsPath:
                    default: /metrics
                    description: MetricsPath override
                    type: string
                  metricsPort:
                    description: MetricsPort override
                    type: integer
                  podLabels:
                    additionalProperties:
                      type: string
                    description: PodLabels to identify metrics endpoints
                    type: object
                  podMonitor:
                    description: PodMonitor generation
                    type: boolean
                  serviceMonitor:
                    description: ServiceMonitor generation
                    type: boolean
                type: object
              parameters:
                description: Parameters that can be passed to this component
                items:
                  description: ComponentParameter defines a parameter that can be
                    passed to the component
                  properties:
                    default:
                      description: Default value
                      type: string
                    description:
                      description: Description of the parameter
                      type: string
                    name:
                      description: Name of the parameter
                      type: string
                    required:
                      description: Required parameter
                      type: boolean
                    type:
                      default: string
                      description: Type of the parameter
                      enum:
                      - string
                      - int
                      - bool
                      type: string
                  required:
                  - name
                  type: object
                type: array
              sources:
                description: Sources define where to find the component
                items:
                  description: ComponentSource defines a source location for a component
                  properties:
                    helm:
                      description: Helm chart configuration
                      properties:
                        parameters:
                          description: Parameters that can be templated
                          items:
                            description: HelmParameter defines a Helm parameter
                            properties:
                              forceString:
                                description: ForceString forces the value to be a
                                  string
                                type: boolean
                              name:
                                description: Name of the parameter
                                type: string
                              value:
                                description: Value or template
                                type: string
                            required:
                            - name
                            - value
                            type: object
                          type: array
                        releaseName:
                          description: ReleaseName override
                          type: string
                        valuesFiles:
                          description: Values file path within the repo
                          items:
                            type: string
                          type: array
                      type: object
                    path:
                      description: Path within the repository
                      type: string
                    repoURL:
                      description: RepoURL is the Git repository URL
                      type: string
                    targetRevision:
                      default: HEAD
                      description: TargetRevision is the Git ref (branch, tag, commit
                        SHA)
                      type: string
                  required:
                  - path
                  - repoURL
                  type: object
                type: array
              type:
                description: 'Type of component: app, workflow, config'
                enum:
                - app
                - workflow
                - config
                type: string
            required:
            - type
            type: object
          status:
            description: ComponentStatus defines the observed state of Component
            properties:
              conditions:
                description: Conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
