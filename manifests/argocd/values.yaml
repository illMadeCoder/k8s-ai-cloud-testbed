# ArgoCD Helm values
# Minimal config for lab use - ArgoCD only, no auto-bootstrap
#
# Experiments define their own app-of-apps to control what gets deployed

server:
  service:
    type: NodePort  # Easy access in minikube, change to LoadBalancer for cloud

# No applications section - experiments manage their own deployments
