version: '3'

silent: true

includes:
  hub: ./platform/cluster/Taskfile.yaml
  talos: ./platform/Taskfile.talos-bootstrap.yaml
  talos-hub: ./platform/Taskfile.talos-hub.yaml

tasks:
  default:
    desc: List available tasks
    cmds:
      - task --list
