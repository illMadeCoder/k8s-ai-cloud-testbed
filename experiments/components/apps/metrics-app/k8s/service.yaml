---
apiVersion: v1
kind: Service
metadata:
  name: metrics-app
  labels:
    app: metrics-app
spec:
  type: LoadBalancer
  selector:
    app: metrics-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
