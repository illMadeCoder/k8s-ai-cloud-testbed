apiVersion: v1
kind: Service
metadata:
  name: cicd-sample
  namespace: cicd-sample
  labels:
    app: cicd-sample
spec:
  selector:
    app: cicd-sample
  ports:
    - name: http
      port: 80
      targetPort: 8080
