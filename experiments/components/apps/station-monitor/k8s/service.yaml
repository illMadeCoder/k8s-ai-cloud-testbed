---
apiVersion: v1
kind: Service
metadata:
  name: station-monitor
  namespace: station
  labels:
    app: station-monitor
spec:
  type: LoadBalancer
  selector:
    app: station-monitor
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
