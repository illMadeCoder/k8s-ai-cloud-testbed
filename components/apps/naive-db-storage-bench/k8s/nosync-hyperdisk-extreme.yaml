apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: naive-db-nosync-hyperdisk-extreme
  labels:
    app: naive-db-nosync-hyperdisk-extreme
spec:
  serviceName: naive-db-nosync-hyperdisk-extreme
  replicas: 1
  selector:
    matchLabels:
      app: naive-db-nosync-hyperdisk-extreme
  template:
    metadata:
      labels:
        app: naive-db-nosync-hyperdisk-extreme
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      securityContext:
        fsGroup: 65532
      containers:
        - name: naive-db
          image: ghcr.io/illmadecoder/naive-db:latest
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: DATA_DIR
              value: /data
            - name: NAIVE_DB_SYNC_MODE
              value: nosync
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 500m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            capabilities:
              drop: [ALL]
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        storageClassName: bench-hyperdisk-extreme
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 64Gi
---
apiVersion: v1
kind: Service
metadata:
  name: naive-db-nosync-hyperdisk-extreme
  labels:
    app: naive-db-nosync-hyperdisk-extreme
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: naive-db-nosync-hyperdisk-extreme
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: naive-db-nosync-hyperdisk-extreme
  labels:
    app: naive-db-nosync-hyperdisk-extreme
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: naive-db-nosync-hyperdisk-extreme
  targetLabels:
    - app
  endpoints:
    - port: http
      interval: 15s
      path: /metrics
