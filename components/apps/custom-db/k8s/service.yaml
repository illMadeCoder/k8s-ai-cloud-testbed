---
apiVersion: v1
kind: Service
metadata:
  name: custom-db
  labels:
    app: custom-db
spec:
  type: ClusterIP
  selector:
    app: custom-db
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
