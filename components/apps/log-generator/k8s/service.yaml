apiVersion: v1
kind: Service
metadata:
  name: log-generator
  labels:
    app: log-generator
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: log-generator
---
apiVersion: v1
kind: Service
metadata:
  name: log-generator-external
  labels:
    app: log-generator
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: log-generator
