apiVersion: experiments.illm.io/v1alpha1
kind: Component
metadata:
  name: naive-db-nosync
spec:
  type: app
  description: "Naive database without fsync — volatile variant for fsync vs in-memory comparison"
  sources:
    - repoURL: https://github.com/illMadeCoder/k8s-ai-cloud-testbed.git
      targetRevision: HEAD
      path: components/apps/naive-db-nosync/k8s
  observability:
    serviceMonitor: true
    podLabels:
      app: naive-db-nosync
    metricsPort: 8080
    metricsPath: /metrics
