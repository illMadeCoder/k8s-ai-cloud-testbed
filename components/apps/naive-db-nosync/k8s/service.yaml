apiVersion: v1
kind: Service
metadata:
  name: naive-db-nosync
  labels:
    app: naive-db-nosync
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: naive-db-nosync
