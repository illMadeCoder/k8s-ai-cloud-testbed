---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: naive-db
  labels:
    app: naive-db
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: naive-db
  endpoints:
    - port: http
      interval: 15s
      path: /metrics
