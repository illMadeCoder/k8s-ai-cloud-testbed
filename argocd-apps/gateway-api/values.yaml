# Contour Helm Values (Official Chart)
# https://projectcontour.io/docs/latest/guides/gateway-api/

# Gateway provisioner configuration
gatewayProvisioner:
  enabled: true

# Envoy configuration
envoy:
  service:
    type: LoadBalancer
